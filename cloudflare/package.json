{
  "name": "doodad-video-processor",
  "version": "1.0.0",
  "description": "Cloudflare Worker for video processing",
  "main": "video-worker.js",
  "scripts": {
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.6",
    "@ffmpeg/util": "^0.12.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
} 