// queer-ai/next.config.mjs
// import type { NextConfig } from "next"; // Remove this TS-specific import

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Example: reactStrictMode: true,
  // Add any specific Next.js config you need, e.g., experimental features, redirects, etc.
};

export default nextConfig;
